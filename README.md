# conthtml — contador.com.py rebuild (HTML + PHP)

Static HTML + PHP site for contador.com.py, design direction 1B, hosted on Hostinger shared hosting.

Start here: `plan.md`. Phases run from `prompts/`. Reference inputs in `docs/`.

Kick off the build in a fresh Opus session:

    Read prompts/opus-1-foundation.md in this repo and execute it.
